import { abort } from "process";
import Toggle from "react-toggle";

export default () => {
  return null;
  return (
    <div
      style={{
        position: "absolute",
        right: 0,
        top: 0,
        marginTop: "2rem",
        marginRight: "2rem",
      }}
    >
      Auto
    </div>
  );
};
